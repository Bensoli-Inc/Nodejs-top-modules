//start by importing global modules exported
const names = require('./names')
const greeting = require('./utils')

greeting(names.bensoli)
greeting(names.peter)
greeting(names.john)
greeting('alex')

